import { StyledFooter } from './styled';

const Footer = () => {
    return (
        <StyledFooter>
            Ecommerce created during Wizeline’s Academy React Bootcamp, México
            2021
        </StyledFooter>
    );
};

export default Footer;
